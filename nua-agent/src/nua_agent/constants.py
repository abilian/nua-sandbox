"""
This defines how the Nua-built applications are structured.

Note that for applications that are built outside Nua (e.g. using their own
Dockerfile or other container image build tool), the structure may differ.
"""

NUA_BUILD_ROOT = "/nua/build"
