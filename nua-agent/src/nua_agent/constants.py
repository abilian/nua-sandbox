"""
This defines how the Nua-built applications are structured.

Note that for applications that are built outside of Nua (e.g. using their own
Dockefile or other container image build tool), the structure may differ.
"""

NUA_BUILD_ROOT = "/nua/build"
